"""ETL Pipeline application package.

Package-level imports are kept minimal to avoid side-effects when importing
submodules (for example, during Alembic autogenerate runs). Import the
FastAPI app explicitly from ``app.main`` only when needed.
"""

__version__ = "1.0.0"